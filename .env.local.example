# ===================
# --- Basic Setup ---
# ===================

# Groq provides fast, cheap transcription AND ad detection.
# For better ad detection accuracy, use xAI Grok instead (see Advanced Setup below).
# Get a Groq key at: https://console.groq.com/keys
# 
# NOTE: Groq (groq.com) is NOT the same as Grok (xAI's model)!
#   - Groq = Fast inference platform (used for Whisper transcription + LLM) - CHEAPER
#   - Grok = xAI's chatbot/LLM (better ad detection, ~$0.10/episode)
#
GROQ_API_KEY=gsk_your_groq_api_key_here

# ======================
# --- Advanced Setup ---
# ======================

# ================================================
# --- LLM for Ad Detection (choose ONE option) ---
# ================================================

# Option 1: Groq (RECOMMENDED - fast, free tier, uses GROQ_API_KEY above)
# No extra config needed - just set GROQ_API_KEY above

# Option 2: xAI Grok (high quality, ~$0.10/episode)
# NOTE: This is Grok (xAI), NOT Groq! Different companies.
# LLM_API_KEY=xai-your-api-key
# LLM_MODEL=xai/grok-3
# OPENAI_BASE_URL is optional — the xai/ prefix auto-routes via LiteLLM

# Option 3: OpenAI (high quality, ~$0.10/episode)
# LLM_API_KEY=sk-your-openai-key
# LLM_MODEL=gpt-4o

# Option 4: Local/custom endpoint (e.g., Ollama)
# LLM_API_KEY=your-key
# LLM_MODEL=your-model
# OPENAI_BASE_URL=http://localhost:11434/v1

# =============================================
# --- Whisper for Transcription (choose ONE) ---
# =============================================

# WHISPER_TYPE options: groq | local | remote
#
# groq   = RECOMMENDED - Fast cloud transcription using Groq (uses GROQ_API_KEY)
# local  = Runs on your CPU/GPU - slow without GPU (~15-30 min per hour of audio)
# remote = OpenAI Whisper API or compatible endpoint

WHISPER_TYPE=groq

# --- Local Whisper (only if WHISPER_TYPE=local) ---
# Models: tiny.en, base.en, small.en, medium.en, large
# Smaller = faster but less accurate. base.en is a good balance.
# WHISPER_LOCAL_MODEL=base.en

# --- Remote Whisper (only if WHISPER_TYPE=remote) ---
# WHISPER_REMOTE_API_KEY=sk-your-openai-key
# WHISPER_REMOTE_BASE_URL=https://api.openai.com/v1
# WHISPER_REMOTE_MODEL=whisper-1
# WHISPER_REMOTE_TIMEOUT_SEC=600
# WHISPER_REMOTE_CHUNKSIZE_MB=24

# --- Groq Whisper (only if WHISPER_TYPE=groq) ---
# Uses GROQ_API_KEY from above. No extra config needed - these are optional:
# GROQ_WHISPER_MODEL=whisper-large-v3-turbo  # (default, no need to set)
# GROQ_MAX_RETRIES=3                          # (default, no need to set)

# ===============
# --- General ---
# ===============

# Port the server listens on (default: 5001)
PORT=5001

# Number of server threads
SERVER_THREADS=1

# =====================
# --- Authentication ---
# =====================

# Set REQUIRE_AUTH=true to enable login for the web UI, feeds, and downloads
# REQUIRE_AUTH=true
# PODLY_ADMIN_USERNAME=podly_admin
# PODLY_ADMIN_PASSWORD=ChangeMe123!  # Minimum 8 characters
# PODLY_SECRET_KEY=replace-with-a-strong-random-value
# SESSION_COOKIE_SECURE=false        # Set to false only for local HTTP (no HTTPS)
